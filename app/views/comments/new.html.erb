<h1>New Comment</h1>

<%= form_with model: @comments, url: comments_create_path, data: {turbo: false} do |form| %>
  <div>
    <%= form.label :text %><br>
    <%= form.text_area :text %>
  </div>

  <div>
    <%= form.submit "Post Comment" %>
  </div>
<% end %>